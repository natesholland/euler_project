// Generated by CoffeeScript 1.10.0
(function() {
  var i, isPalindrome, j, k, l, maxPalindrome;

  isPalindrome = function(x) {
    var firstHalf, secondHalf;
    x = x.toString();
    firstHalf = x.slice(0, +(Math.floor(x.length / 2) - 1) + 1 || 9e9);
    secondHalf = x.slice(Math.ceil(x.length / 2), +(x.length - 1) + 1 || 9e9);
    return firstHalf === secondHalf.split("").reverse().join("");
  };

  maxPalindrome = 0;

  for (i = k = 100; k <= 999; i = ++k) {
    for (j = l = 100; l <= 999; j = ++l) {
      if (isPalindrome(i * j)) {
        if ((i * j) > maxPalindrome) {
          maxPalindrome = i * j;
        }
      }
    }
  }

  print(maxPalindrome);

}).call(this);
